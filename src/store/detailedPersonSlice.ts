// import { createSlice, PayloadAction } from '@reduxjs/toolkit';
// import { Person } from '../types';
// import { DetailedPersonState } from './types';

// export const initialState: DetailedPersonState = {
//   person: null,
// };

// export const detailedPersonSlice = createSlice({
//   name: 'detailedPerson',
//   initialState: initialState,
//   reducers: {
//     setCurrentPerson(state, action: PayloadAction<Person>) {
//       state.person = action.payload;
//     },
//     clearCurrentPerson(state) {
//       state.person = null;
//     },
//   },
// });

// export const { setCurrentPerson, clearCurrentPerson } = detailedPersonSlice.actions;
// export const detailedPersonReducer = detailedPersonSlice.reducer;
